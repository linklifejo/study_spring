<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="co">

<!-- 신규고객정보 삽입저장 -->
<insert id='insert'>
insert into course(name, type, location_id)
values (#{name}, #{type}, #{location_id})
</insert>

<!-- 해당고객정보 수정저장 -->
<update id='update'>
update course
set name = #{name}, type = #{type}, location_id = #{location_id}
where id = #{id}
</update>

<!-- 해당고객정보 삭제 -->
<delete id='delete'>
delete from course where id = #{id}
</delete>

<!-- 해당고객정보조회 -->
<select id='info' resultType='course.CourseVO'>
select c.*,l.name location_name 
from course c inner join location l on c.location_id = l.id
where id = #{id}
</select>

<!-- 고객목록조회 -->
<select id='list' resultType='course.CourseVO'>
select * from course order by name
</select>

</mapper>  